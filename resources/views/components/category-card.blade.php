<article>
    <img src="{{ asset('storage/' . $category->image) }}" alt="" class="mx-auto block">
    <h4 class="text-center text-xl font-semibold mt-2">{{ $category->categories_name }}</h4>
</article>
